name: Build Release Zip

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create zip
        run: |
          zip -r celestial-lunar-phase-${GITHUB_REF_NAME}.zip . \
            -x "*.git*" ".github/*" "docs/*"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: celestial-lunar-phase-${{ github.ref_name }}
          path: celestial-lunar-phase-${{ github.ref_name }}.zip
